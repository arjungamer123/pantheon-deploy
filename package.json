{
  "name": "pantheon-deploy",
  "version": "1.0.0",
  "private": true,
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "echo 'build complete'",
    "start": "npx paperclipai run --config ./paperclip.json --repair"
  },
  "dependencies": {
    "paperclipai": "latest"
  }
}
